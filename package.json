{
    "name": "pmv-si-2026-1-pe6-t1-t1-g5-pet-shop",
    "version": "1.0.0",
    "description": "Pet Shop Management System",
    "private": true,
    "workspaces": [
        "src/pet_flow_backend"
    ],
    "scripts": {
        "install:all": "npm install && cd src/pet_flow_backend && npm install",
        "prepare": "if [ -f node_modules/.bin/husky ]; then husky; fi",
        "build": "cd src/pet_flow_backend && npm run build"
    },
    "devDependencies": {
        "husky": "^9.1.5",
        "ts-node": "^10.9.2",
        "typescript": "^6.0.2"
    }
}
